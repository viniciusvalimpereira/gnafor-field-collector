# Screenshots

Two screenshots are referenced by the README and the manuscript (Section 2.4 /
Response §5.1). Capture them from your own Google account and save them here
with these exact names:

| File | What it should show |
|---|---|
| `01_apps_script_editor.png` | The Apps Script editor with `GoogleAppsScript_doPost.gs` pasted into `Code.gs`. |
| `02_deployment_dialog.png`  | The **Deploy → New deployment → Web app** dialog with **Execute as: Me** and **Who has access: Anyone**. |

(These are the only two assets that must be captured by the author; everything
else in the repository is generated or authored in code.)
